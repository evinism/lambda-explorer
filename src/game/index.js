/*
const state = {};


// fake redux-- really just a
const transforms = {
  'init': () => ({
    currentProblem: 0,
    definedVars: [], // so if the current problem has defined vars, they go here.
    completedProblems: [],
  })
};

const reduce = (state, action) => transforms[action.type](state, action);
*/
